__pycache__/
.ipynb_checkpoints/
.venv/
.DS_Store
names.txt

# Everything below is deliberately tracked in this repository, unlike the
# upstream starter which git-ignores it: corpus/ holds the teaching material
# this experiment authored, and llm_runs/ + results/ are the graded evidence.
